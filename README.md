# tsv1 trigger-strategy GitHub runner

Minimal public runner for the trigger-strategy analyzer.

The analyzer data is hosted separately on Hugging Face:

https://huggingface.co/datasets/RooseveltHonaker/tsv1

Normal use is through the private local launcher in
`trigger_strategy/github_scale_version/github_scale_trigger.py`.
